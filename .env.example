# DEV mode credentials — copy to `.env` and fill in your lockedin account.
# `uv run lockedin devmode` reads these; an agent (Claude Code) uses them to scope itself to
# your workspace (data/users/<DEV_USERNAME>/). The real `.env` is git-ignored.
DEV_USERNAME=your-username
DEV_PASSWORD=your-password
